import React from 'react'
import { Text, View } from 'react-native'

const saved = () => {
  return (
    <View className="flex-1 items-center justify-center bg-primary">
          <Text className="text-5xl text-center font-bold text-accent">
        Saved Items!
      </Text>
    </View>
  )
}

export default saved
